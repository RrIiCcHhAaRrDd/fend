import { toggleFieldfn } from '../src/client/js/toggleField'

describe("testing toggleField fn", () =>{
    test("testing if toggleField fn checks attribute ", () => {
        

        expect(toggleFieldfn).toBeDefined();
    } )
} )